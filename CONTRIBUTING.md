# 参与贡献

欢迎提交问题和改进建议。修改代码前请先说明要解决的具体问题，协议变化请附上去除账号、姓名、课程、坐标和 Cookie 后的最小响应样例。

开发环境需要 Node.js 18 或更高版本：

```bash
npm test
npm run check
```

提交前请确认：

- 测试和语法检查通过。
- 没有提交 Cookie、账号、真实姓名、课程信息、签到活动 ID 或精确坐标。
- 不把未确认的服务器响应显示为签到成功。
- 不加入自动提交、伪造客户端设备身份或绕过验证码、人脸和位置授权的功能。
