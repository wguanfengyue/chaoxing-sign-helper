# 更新记录

## 3.5.0

- 将位置签到改为超星官方定位流程，避免旧接口触发 `locationAuthError_LCR007`。
- 保存活动列表返回的 `data.ext`，并以 POST 表单交给官方预签到页。
- 在操作前显示服务器公开的位置范围和附加校验。
- 加入基于首页课表的课程选择和上下课时间窗口检测。
- 加入本机位置预设、地图选点、地点搜索和地址反查。
- 增加登录态、接口响应、提交结果和超时处理。

## 3.0.0

- 从 Java 命令行工具重写为浏览器用户脚本。
- 复用浏览器现有登录态，不要求用户复制 Cookie 或在脚本中输入密码。
